<Window x:Class="TextRedactor.Font"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TextRedactor"
        mc:Ignorable="d"
        Title="Font" Height="300" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Content="Font:" Grid.Row="0" Grid.Column="0"/>
        <ComboBox x:Name="fontComboBox" Grid.Row="0" Grid.Column="1" Width="120" SelectionChanged="fontComboBox_SelectionChanged">
            <ComboBoxItem Content="Arial"/>
            <ComboBoxItem Content="Times New Roman"/>
            <ComboBoxItem Content="Verdana"/>
        </ComboBox>
        <Label Content="Font Size:" Grid.Row="1" Grid.Column="0"/>
        <TextBox x:Name="fontSizeTextBox" Grid.Row="1" Grid.Column="1" Width="120" TextChanged="fontSizeTextBox_TextChanged"/>
        <RichTextBox x:Name="richTextBox" Grid.Row="2" Grid.ColumnSpan="2"/>
    </Grid>
</Window>
